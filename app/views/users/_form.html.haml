= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email, placeholder: "Podaj Adres Email", label: false

  .form-actions
    = f.button :submit, 'ZAPISZ SIĘ TERAZ!', class: "#{@ab_submit_button_type == 'warning' ? 'btn btn-warning' : 'btn btn-danger'}"