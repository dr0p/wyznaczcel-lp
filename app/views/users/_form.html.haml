= simple_form_for(@user) do |f|
  = f.error_notification

  .form-inputs
    = f.input :email, placeholder: "twoj@email.com", label: false

  .form-actions
    = f.button :submit, 'ZAPISZ SIĘ TERAZ!', class: "btn btn-danger"